let currentConfig;
function drawLab(){const N=+document.querySelector('#count').value,A=+document.querySelector('#amplitude').value,start=+document.querySelector('#start').value;currentConfig=DATA.configs.find(c=>c.N===N&&c.A===A&&c.start===start);const c=currentConfig,base=DATA.configs.find(c=>c.N===N&&c.A===A&&c.start===0),cv=document.querySelector('#lab'),ctx=cv.getContext('2d'),light=document.documentElement.dataset.theme==='light';ctx.fillStyle=light?'#eef3f7':'#142c3b';ctx.fillRect(0,0,1100,350);ctx.font='18px sans-serif';ctx.fillStyle=light?'#263b50':'#a6bccd';for(const y of [-1,0,1]){const py=160-y*125;ctx.fillText(String(y),12,py+6);ctx.strokeStyle=light?'#c5d3df':'#355263';ctx.beginPath();ctx.moveTo(50,py);ctx.lineTo(1070,py);ctx.stroke();}function plot(vals,col){ctx.strokeStyle=col;ctx.lineWidth=3;ctx.beginPath();vals.forEach((v,i)=>{const x=50+i/(N-1)*1020,y=160-v*125;i?ctx.lineTo(x,y):ctx.moveTo(x,y);});ctx.stroke();}plot(base.x,light?'#66758b':'#a1b4c7');plot(c.x,light?'#985000':'#ffc66f');ctx.fillText('0',50,323);ctx.fillText(String(N-1)+' 位置',985,323);document.querySelector('#metrics').textContent=`N=${N}；首样本 ${c.x[0].toFixed(6)}；末点时间 ${c.t[N-1].toFixed(2)}s；平方和 ${c.energy.toFixed(9)}；相对基准最大差 ${Math.max(...c.x.map((v,i)=>Math.abs(v-base.x[i]))).toFixed(6)}`;document.querySelector('#legend').textContent='灰：同长度、同幅度的起点0基准；金：当前选择。两线重合时只看到金线。';}
for(const id of ['count','amplitude','start'])document.getElementById(id).addEventListener('change',drawLab);
document.querySelector('#quiz').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target),keys=['q1','q2','q3'],answers=['b','a','b'];document.querySelector('#feedback').textContent=keys.some(k=>!f.get(k))?'请先完成三道题。':keys.map((k,i)=>`${i+1}. ${f.get(k)===answers[i]?'正确':'请再想一想'}`).join('；');});
