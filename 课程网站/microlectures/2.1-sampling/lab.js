let currentConfig;
function drawLab(){const fs=+document.querySelector('#fs').value,kind=document.querySelector('#kind').value;currentConfig=DATA.configs.find(c=>c.fs===fs&&c.kind===kind);const c=currentConfig,cv=document.querySelector('#lab'),ctx=cv.getContext('2d'),light=document.documentElement.dataset.theme==='light';ctx.fillStyle=light?'#eef3f7':'#142c3b';ctx.fillRect(0,0,1100,350);ctx.font='18px sans-serif';ctx.fillStyle=light?'#263b50':'#a6bccd';for(const y of [-1,0,1]){const py=160-y*125;ctx.fillText(String(y),12,py+6);ctx.strokeStyle=light?'#c5d3df':'#355263';ctx.beginPath();ctx.moveTo(50,py);ctx.lineTo(1070,py);ctx.stroke();}function plot(vals,col){ctx.strokeStyle=col;ctx.lineWidth=3;ctx.beginPath();vals.forEach((v,i)=>{const x=50+DATA.tc[i]*1020,y=160-v*125;i?ctx.lineTo(x,y):ctx.moveTo(x,y);});ctx.stroke();}plot(c.wave,light?'#66758b':'#a1b4c7');plot(c.alias_wave,light?'#985000':'#ffc66f');ctx.strokeStyle=ctx.fillStyle=light?'#007b6c':'#4dd7c2';for(let i=0;i<c.x.length;i++){const x=50+c.t[i]*1020,y=160-c.x[i]*125;ctx.beginPath();ctx.moveTo(x,160);ctx.lineTo(x,y);ctx.stroke();ctx.beginPath();ctx.arc(x,y,4,0,Math.PI*2);ctx.fill();}ctx.fillText('0',50,323);ctx.fillText('1 s',1020,323);document.querySelector('#metrics').textContent=`fs=${fs}Hz；样本数${c.x.length}（含两端点）；带符号折叠频率${c.fold}Hz；候选样本最大差${c.alias_error.toExponential(2)}；样本最大绝对值${Math.max(...c.x.map(Math.abs)).toFixed(6)}`;document.querySelector('#legend').textContent=kind==='sin'&&fs===6?'灰：5Hz正弦；金：−1Hz正弦（等于+1Hz正弦取负）；绿：样本。':'灰：原信号；金：带符号折叠频率的候选；绿：样本。重合时部分曲线会被覆盖。';}
for(const id of ['fs','kind'])document.getElementById(id).addEventListener('change',drawLab);
document.querySelector('#quiz').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target),keys=['q1','q2','q3'],answers=['b','a','b'];document.querySelector('#feedback').textContent=keys.some(k=>!f.get(k))?'请先完成三道题。':keys.map((k,i)=>`${i+1}. ${f.get(k)===answers[i]?'正确':'请再想一想'}`).join('；');});
