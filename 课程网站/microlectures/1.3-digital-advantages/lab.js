let currentConfig;
function drawLab(){const bits=+document.querySelector('#bits').value,gain=+document.querySelector('#gain').value;currentConfig=DATA.configs.find(c=>c.bits===bits&&c.gain===gain);const c=currentConfig,cv=document.querySelector('#lab'),ctx=cv.getContext('2d'),light=document.documentElement.dataset.theme==='light';ctx.fillStyle=light?'#eef3f7':'#142c3b';ctx.fillRect(0,0,1100,350);ctx.font='18px sans-serif';ctx.fillStyle=light?'#263b50':'#a6bccd';for(const y of [-2,-1,0,1,2]){const py=160-y*65;ctx.fillText(String(y),12,py+6);ctx.strokeStyle=light?'#c5d3df':'#355263';ctx.beginPath();ctx.moveTo(50,py);ctx.lineTo(1070,py);ctx.stroke();}function plot(vals,col){ctx.strokeStyle=col;ctx.lineWidth=3;ctx.beginPath();vals.forEach((v,i)=>{const x=50+i/31*1020,y=160-v*65;i?ctx.lineTo(x,y):ctx.moveTo(x,y);});ctx.stroke();}plot(c.ideal,light?'#66758b':'#a1b4c7');plot(c.stored,light?'#985000':'#ffc66f');ctx.fillText('0',50,323);ctx.fillText('31  n',1010,323);document.querySelector('#metrics').textContent=`输入量化最大误差 ${c.max_error.toFixed(6)} / 界限 ${(c.delta/2).toFixed(6)}；输出越界 ${c.overload_count} 个；重复计算差 ${c.repeat_error}`;document.querySelector('#legend').textContent='灰：理想乘法输出；金：重新量化并限制范围后的存储输出。';}
for(const id of ['bits','gain'])document.getElementById(id).addEventListener('change',drawLab);
document.querySelector('#quiz').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target),keys=['q1','q2','q3'],answers=['b','a','b'];document.querySelector('#feedback').textContent=keys.some(k=>!f.get(k))?'请先完成三道题。':keys.map((k,i)=>`${i+1}. ${f.get(k)===answers[i]?'正确':'请再想一想'}`).join('；');});
